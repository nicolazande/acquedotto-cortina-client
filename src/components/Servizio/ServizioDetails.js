import React from 'react';
import { detailViews } from '../../config/detailViews';
import DetailPage from '../shared/DetailPage';

const ServizioDetails = () => <DetailPage config={detailViews.servizi} />;

export default ServizioDetails;
