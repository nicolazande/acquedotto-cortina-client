import React from 'react';
import { detailViews } from '../../config/detailViews';
import DetailPage from '../shared/DetailPage';

const LetturaDetails = () => <DetailPage config={detailViews.letture} />;

export default LetturaDetails;
